ANTHROPIC_API_KEY=sk-ant-your-key-here

# SQLite is used by default (no server required).
# SQLITE_PATH=./data/log-analyzer.db

# Optional Redis cache (gracefully skipped if not available)
REDIS_URL=redis://localhost:6379

PORT=3000
NODE_ENV=development
